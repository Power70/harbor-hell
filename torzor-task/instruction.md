Create a file at /app/output.txt containing exactly the following text:

harbor-hell: task completed

The agent should create any parent directories needed and ensure the file contains exactly the text above (no extra whitespace). Write the file so that tests can read it at `/app/output.txt`.
